node_modules/
dist/
*.log
.env
.env.local
.sandbox/
workspace/.draft/
workspace/logs/
.toaa/
coverage/
.tools/

# # release artifacts (binaries are too large for git; track only release notes + checksums)
# release/**/*.tar.gz
# release/**/*.zip

node_modules/
dist/
*.log
.env
.env.local
.sandbox/
workspace/.draft/
workspace/logs/
.toaa/
coverage/
.tools/

# release/ 目录由 .github/workflows/release.yml 在 tag 推送时通过 GitHub Releases 发布，
# 不再纳入版本库（历史中曾短暂入库，已通过 git rm 移除，commit 历史保留）。
release/
